# Env examples: use .env.local for dev, .env.prod for production (see below).
# Never commit .env, .env.local, or .env.prod.
#
# Local dev:  copy .env.local.example → .env.local
# Production: copy .env.prod.example   → .env.prod (on VM)
#
# One-off / legacy: copy this file to .env if not using env-file overrides.

DB_HOST=mysql
DB_PORT=3306
DB_NAME=nba_flight_tracker
DB_USER=flight_user
DB_PASSWORD=change_me_use_strong_password

